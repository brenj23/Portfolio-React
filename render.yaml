services:
  - type: web
    name: react-portfolio
    env: node
    plan: free
    buildCommand: npm i && npm run build
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
